<div class="container">
  <h1>all users!!</h1>

  <% @users.each do |user| %>
  
    <li><%= user.email %></li>
    <%= link_to :delete, user_registration_path(id: user), class: "btn btn-danger btn-xs", method: :delete %>
  <% end %>